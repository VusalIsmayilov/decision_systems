SECTION: Hero
Page: Decision Problem
Pattern: Situational → Entry Trigger Field
Objective: Surface recurring inconsistency as an observable condition and anchor entry into pattern recognition

Layout Logic:
• Entry zone (left-heavy dominant field):
Primary element:

* Eyebrow: Decision Consistency
* Headline: The same decision should not produce different outcomes.

• Processing zone (right fragmented cluster – grouped signals, not equal):
Supporting elements:

* The same business decision is made repeatedly across your organization.

* Yet each time, it produces a different outcome.

* Different teams interpret the same situation differently.

* The same numbers lead to different actions.

* Decisions depend on who is involved.

* This is not isolated. It repeats.

• Exit / transition zone (bottom-right directional push):
Control element:

* Downward scroll trigger (no alternative paths)

Hierarchy:
• Headline dominates left 60–70% width
• Supporting statements fragmented into clustered right-side blocks (not linear list)
• Final line isolated as compression anchor (high emphasis, low width)

Interaction:
• Scroll: free → slight snap at end of section
• No hover interactions
• Content exposure: static (no progressive reveal)

Navigation Behavior:
• Linear progression only
• Forward movement via scroll
• No lateral exits
• Exit leads directly to “Observable patterns” section

Transition Role:
• Establishes observable inconsistency as entry condition
• Pushes user from recognition → pattern identification

ASCII WIREFRAME:

```
[ ENTRY ZONE — LEFT DOMINANT ]

Decision Consistency

THE SAME DECISION SHOULD NOT PRODUCE
DIFFERENT OUTCOMES



[ PROCESSING ZONE — RIGHT FRAGMENTED ]

The same business decision is made
repeatedly across your organization.

Yet each time, it produces a different outcome.


Different teams interpret the same situation differently.
The same numbers lead to different actions.
Decisions depend on who is involved.


                This is not isolated.
                It repeats.



[ EXIT / TRANSITION — BOTTOM RIGHT ]

↓
(scroll)
→ Observable patterns
```
_______________


SECTION: Observable patterns
Page: Decision Problem
Pattern: Recognition → Linear Scan
Objective: Convert the repeated inconsistency introduced in Hero into a scannable pattern field that confirms recurrence across functions

Layout Logic:
• Entry zone (narrow left anchor):
Primary element:

* Headline: It shows up the same way every time

• Processing zone (dominant right linear scan with controlled interruption):
Supporting elements:

* The same discussion returns in every cycle
* Teams align on data, but not on action
* One team acts while another delays
* Decisions change when ownership changes
* Reports explain performance, but do not guide action
* No one can describe how the decision is supposed to work

Supporting anchor:

* These patterns are consistent across functions, not local to one team.

• Exit / transition zone (bottom forward strip):
Control element:

* Downward transition to Implication

Hierarchy:
• Section headline fixed as left-side recognition anchor
• Pattern lines stacked in dominant right column as sequential scan units
• Supporting anchor placed outside main scan at lower edge to widen scope
• Transition strip isolated below content to force continuation

Interaction:
• Scroll: free with light snap on transition strip
• Hover: optional row highlight only on each pattern line
• Click: none
• Content exposure logic: static, all pattern lines visible at once

Navigation Behavior:
• Linear progression only
• User moves next by scroll
• No optional branches
• Transition leads directly to “Implication”

Transition Role:
• Confirms that inconsistency is systemic and recurring
• Pushes from observed signals to recurring-condition interpretation

ASCII WIREFRAME:

```text
[ ENTRY ZONE ]                         [ PROCESSING ZONE — DOMINANT LINEAR SCAN ]

It shows up the same way              01  The same discussion returns
every time                                in every cycle

                                      02  Teams align on data,
                                          but not on action

                                      03  One team acts while
                                          another delays

                                      04  Decisions change when
                                          ownership changes

                                      05  Reports explain performance,
                                          but do not guide action

                                      06  No one can describe how
                                          the decision is supposed to work



[ LOWER SUPPORTING ANCHOR — OFFSET ]
These patterns are consistent across functions,
not local to one team.



[ EXIT / TRANSITION STRIP ]
↓
Implication
```

 
______________


SECTION: Implication
Page: Decision Problem
Pattern: Evaluation → Binary Decision Frame
Objective: Convert observed repetition into a judgment condition and remove incidental interpretation

Layout Logic:
• Entry zone (upper-left framing anchor):
Primary element:

* Headline: This is recurring, not incidental

• Processing zone (asymmetrical binary frame, right-dominant conclusion):
Supporting elements:

* When the same type of decision produces different outcomes under the same conditions,
* the issue is not isolated variation.
* It is a recurring condition.

• Exit / transition zone (bottom directional release):
Control element:

* Forward transition to “What explains this pattern”

Hierarchy:
• Headline isolated as evaluative frame at top-left
• Conditional statement placed as left-side validation input
• “the issue is not isolated variation.” positioned as rejection block
• “It is a recurring condition.” isolated in dominant right-side conclusion block

Interaction:
• Scroll: free entry, snap on conclusion block
• Hover: none
• Click: none
• Content exposure logic: progressive reveal by scroll threshold

* first: headline
* second: condition statement
* third: rejection block + conclusion block

Navigation Behavior:
• Linear progression only
• Transition triggered by downward scroll after conclusion block enters full view
• No optional paths
• Next section: Transition

Transition Role:
• Closes pattern recognition with explicit judgment
• Forces movement from repeated signals to search for underlying explanation

ASCII WIREFRAME:

```text
[ ENTRY ZONE ]

Implication

THIS IS RECURRING,
NOT INCIDENTAL



[ PROCESSING ZONE — BINARY DECISION FRAME ]

When the same type of decision produces
different outcomes under the same conditions,

┌───────────────────────────────┐         ┌───────────────────────────────┐
│ the issue is not              │   →     │ It is a recurring condition.  │
│ isolated variation.           │         │                               │
└───────────────────────────────┘         └───────────────────────────────┘



[ EXIT / TRANSITION ZONE ]

↓
What explains this pattern
```

 
_______________


SECTION: Transition
Page: Decision Problem
Pattern: Transition → Forward Push
Objective: Move from confirmed recurrence to structural inquiry and direct the user into the next page without ambiguity

Layout Logic:
• Entry zone (compressed upper-left anchor):
Primary element:

* Headline: What explains this pattern

• Processing zone (forward-driving asymmetrical push field):
Supporting elements:

* If inconsistency repeats across decisions, teams, and time,
* what is driving it is not immediately visible.
* It is not contained in data, tools, or individual behavior.

• Exit / transition zone (dominant lower-right directional endpoint):
Control element:

* Forward path to next page: Decision Systems

Hierarchy:
• Headline isolated as transition trigger
• Conditional line placed as left-side premise
• “what is driving it is not immediately visible.” centered as directional bridge
• “It is not contained in data, tools, or individual behavior.” placed in dominant endpoint block before page transition
• Next-page control isolated at lower-right edge

Interaction:
• Scroll: progressive reveal with end snap
• Hover: none
• Click: next-page endpoint clickable
• Content exposure logic:

* first: headline
* second: premise line
* third: bridge statement
* fourth: endpoint statement + page transition control

Navigation Behavior:
• Linear progression with one explicit forward destination
• Transition triggered by scroll completion or click on endpoint control
• No optional supporting paths
• User goes next to: Decision Systems

Transition Role:
• Ends page-level recognition sequence
• Pushes user from observed inconsistency into structural cause framing on the next page

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

What explains
this pattern



[ PROCESSING ZONE — FORWARD PUSH ]

If inconsistency repeats across
decisions, teams, and time,

                    what is driving it
                    is not immediately visible.



[ DIRECTIONAL ENDPOINT — LOWER RIGHT DOMINANT ]

It is not contained in data,
tools, or individual behavior.


                    ┌──────────────────────┐
                    │ Decision Systems  →  │
                    └──────────────────────┘
```

 
_____________

SECTION: Hero
Page: Decision Systems
Pattern: Contrast → Asymmetric Split
Objective: Reframe the problem from information access to structural definition and establish the dominant causal distinction for the page

Layout Logic:
• Entry zone (left lighter premise column):
Primary element:

* Eyebrow: Structural Cause
* Organizations operate with data, dashboards, and reporting.
* Yet the same decisions continue to produce different outcomes.

• Processing zone (right dominant reframing block):
Supporting elements:

* Headline: Inconsistency persists because decisions are not structurally defined
* The issue is not access to information.
* The issue is that the decision itself has no defined structure.

• Exit / transition zone (lower-right continuation control):
Control element:

* Downward progression to “Mechanism of failure”

Hierarchy:
• Right-side headline is dominant and carries primary interpretive weight
• Left-side premise cluster provides contrast context, not equal balance
• Reframe statements are split into two stacked decision lines under headline
• Exit control isolated at lower-right to preserve single forward path

Interaction:
• Scroll: progressive reveal with soft snap at section end
• Hover: none
• Click: none within content; transition strip may be clickable as scroll aid
• Content exposure logic:

* first: eyebrow + left premise
* second: dominant headline
* third: two reframe lines
* fourth: transition control

Navigation Behavior:
• Linear progression only
• User moves next by scroll or click on transition strip
• No lateral branches
• Next destination: Mechanism of failure

Transition Role:
• Converts the prior page question into a structural answer frame
• Pushes from surface inconsistency into explanation of how failure operates

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT / LIGHTER ]              [ PROCESSING ZONE — RIGHT / DOMINANT ]

Structural Cause                            INCONSISTENCY PERSISTS
                                            BECAUSE DECISIONS ARE NOT
Organizations operate with data,            STRUCTURALLY DEFINED
dashboards, and reporting.

Yet the same decisions continue             The issue is not access
to produce different outcomes.              to information.

                                            The issue is that the decision
                                            itself has no defined structure.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Mechanism of failure
```

 
_____________



SECTION: Mechanism of failure
Page: Decision Systems
Pattern: Breakdown → Fragmented Grid
Objective: Expose the repeated failure mechanism as a set of unstable decision elements and make absence of stable structure visible

Layout Logic:
• Entry zone (upper-left anchor block):
Primary element:

* Headline: Decisions are reconstructed every time
* Recurring decisions are treated as events.

• Processing zone (dominant fragmented grid with uneven blocks):
Supporting elements:

* Each time they occur:
* the trigger is unclear
* inputs vary
* logic is interpreted
* ownership shifts
* workflow changes
* outcomes are not tracked

• Exit / transition zone (lower-right compression endpoint):
Control element:

* There is no stable structure.
* Forward progression to “Why existing efforts fail”

Hierarchy:
• Headline and framing statement grouped as single anchor in entry zone
• “Each time they occur:” acts as grid trigger, separated from item field
• Failure points distributed across uneven 2-row fragmented grid, not stacked list
• “There is no stable structure.” isolated as terminal conclusion block with highest endpoint emphasis

Interaction:
• Scroll: progressive reveal across grid, end snap on conclusion block
• Hover: optional light emphasis on individual failure blocks
• Click: none on failure blocks
• Content exposure logic:

* first: headline + framing statement
* second: “Each time they occur:”
* third: fragmented grid items
* fourth: conclusion block + next-section transition

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on lower-right next-section control
• No optional paths
• Next destination: Why existing efforts fail

Transition Role:
• Converts structural reframing into operational breakdown
• Pushes from instability field to explanation of why data, reporting, and process do not resolve it

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Mechanism of failure

DECISIONS ARE RECONSTRUCTED
EVERY TIME

Recurring decisions are treated as events.



[ PROCESSING ZONE — FRAGMENTED GRID ]

Each time they occur:

┌──────────────────────┐      ┌──────────────────────┐      ┌──────────────────────┐
│ the trigger          │      │ inputs vary          │      │ logic is interpreted │
│ is unclear           │      │                      │      │                      │
└──────────────────────┘      └──────────────────────┘      └──────────────────────┘

         ┌──────────────────────┐      ┌──────────────────────┐      ┌──────────────────────────┐
         │ ownership shifts     │      │ workflow changes     │      │ outcomes are not tracked │
         └──────────────────────┘      └──────────────────────┘      └──────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

┌──────────────────────────────┐
│ There is no stable structure.│
└──────────────────────────────┘

                    ↓
         Why existing efforts fail
```

 
______________


SECTION: Why existing efforts fail
Page: Decision Systems
Pattern: Contrast → Asymmetric Split
Objective: Separate what organizations rely on from what those efforts do not define, and drive forward into the reframe

Layout Logic:
• Entry zone (left indexed premise field):
Primary element:

* Headline: Data, reporting, and process do not define decisions

• Processing zone (right dominant contrast field with paired blocks):
Supporting elements:

* Data provides inputs, not decisions
* Dashboards display information, not logic
* Process defines steps, not evaluation criteria
* Governance assigns responsibility, not how decisions are made

• Exit / transition zone (bottom-right conclusion lock):
Control element:

* None of these determine how a decision works.
* Forward progression to Reframe

Hierarchy:
• Headline isolated on left as contrast anchor
• Four paired statements grouped in right dominant field as compressed contrast units
• Each pair preserves “X / not Y” logic without turning into equal stacked paragraphs
• Conclusion block separated below the pairs as terminal judgment
• Next-step control attached to conclusion block only

Interaction:
• Scroll: progressive reveal by pair group, end snap on conclusion block
• Hover: optional emphasis on individual contrast pairs
• Click: none within content blocks
• Content exposure logic:

* first: headline
* second: first two pairs
* third: next two pairs
* fourth: conclusion block + next-section control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on Reframe control
• No optional branches
• Next destination: Reframe

Transition Role:
• Prevents misattribution of failure to data, dashboards, process, or governance
• Pushes user into explicit problem definition on the next section

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT / LIGHTER ]

Why existing efforts fail

DATA, REPORTING, AND
PROCESS DO NOT
DEFINE DECISIONS



[ PROCESSING ZONE — RIGHT / DOMINANT CONTRAST FIELD ]

┌──────────────────────────────┐      ┌────────────────────────────────────┐
│ Data provides inputs,        │  /   │ not decisions                     │
└──────────────────────────────┘      └────────────────────────────────────┘

┌──────────────────────────────┐      ┌────────────────────────────────────┐
│ Dashboards display           │  /   │ information, not logic            │
│ information                  │      │                                    │
└──────────────────────────────┘      └────────────────────────────────────┘

         ┌──────────────────────────────┐      ┌──────────────────────────────┐
         │ Process defines steps,       │  /   │ not evaluation criteria      │
         └──────────────────────────────┘      └──────────────────────────────┘

         ┌──────────────────────────────┐      ┌──────────────────────────────┐
         │ Governance assigns           │  /   │ not how decisions are made   │
         │ responsibility               │      │                              │
         └──────────────────────────────┘      └──────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

┌────────────────────────────────────────────┐
│ None of these determine how a decision     │
│ works.                                     │
└────────────────────────────────────────────┘

                              ↓
                           Reframe
```

 

_____________

SECTION: Reframe
Page: Decision Systems
Pattern: Definition → Anchored Block
Objective: State the actual problem directly and lock the page into a single structural interpretation before advancing to category definition

Layout Logic:
• Entry zone (dominant anchored core):
Primary element:

* Headline: The problem is not information or discipline
* The problem is that the decision itself is undefined.

• Processing zone (offset lower-right continuation block):
Supporting elements:

* Until the structure of the decision is explicit,
* consistent outcomes cannot emerge.

• Exit / transition zone (compressed forward lock):
Control element:

* Forward progression to next page: Decision Systems Design

Hierarchy:
• Headline is the dominant anchor and occupies primary visual weight
• Second sentence sits directly beneath as the defining statement
• Conditional statement is displaced into lower-right supporting block to create forward pull
• Transition control is isolated below the support block as the only next action

Interaction:
• Scroll: free entry, snap on anchored core, release on transition control
• Hover: none
• Click: next-page control clickable
• Content exposure logic: static anchor first, supporting block enters on continued scroll

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on next-page control
• No optional supporting paths
• Next destination: Decision Systems Design

Transition Role:
• Ends reframing sequence with a fixed definition
• Pushes user from problem definition into system category formation

ASCII WIREFRAME:

```text
[ ENTRY ZONE — ANCHORED CORE ]

Reframe

THE PROBLEM IS NOT
INFORMATION OR DISCIPLINE

The problem is that the decision
itself is undefined.



[ PROCESSING ZONE — OFFSET SUPPORT ]

                         Until the structure of the
                         decision is explicit,

                         consistent outcomes
                         cannot emerge.



[ EXIT / TRANSITION ZONE ]

                         ┌──────────────────────────────┐
                         │ Decision Systems Design   →  │
                         └──────────────────────────────┘
```

 
____________


SECTION: Hero
Page: Decision Systems Design
Pattern: Definition → Anchored Block
Objective: Establish the category definition clearly and move from undefined decision to explicit system structure

Layout Logic:
• Entry zone (dominant anchored core on left):
Primary element:

* Eyebrow: Decision System Design
* Headline: A decision becomes reliable when its structure is explicit

• Processing zone (offset right support field):
Supporting elements:

* A recurring business decision can be defined as a system.
* When its structure is specified,
* it produces consistent outcomes under the same conditions.

• Exit / transition zone (lower-right directional release):
Control element:

* Downward progression to Structure

Hierarchy:
• Headline is the primary anchor and carries the section weight
• Eyebrow sits above as category cue only
• Supporting statement is split into short units in the offset field to avoid paragraph block
• Exit control is isolated and subordinate, preserving one forward path

Interaction:
• Scroll: free entry, soft snap on anchor, release on exit
• Hover: none
• Click: none inside content; transition strip may be clickable
• Content exposure logic: static anchor first, support field fully visible within same fold

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on transition strip
• No optional branches
• Next destination: Structure

Transition Role:
• Converts problem definition into category definition
• Pushes user from conceptual statement to explicit structural breakdown

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT DOMINANT ]

Decision System Design

A DECISION BECOMES RELIABLE
WHEN ITS STRUCTURE
IS EXPLICIT



[ PROCESSING ZONE — RIGHT OFFSET ]

A recurring business decision
can be defined as a system.

When its structure is specified,

it produces consistent outcomes
under the same conditions.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Structure
```

 
___________



SECTION: Structure
Page: Decision Systems Design
Pattern: Structure → Indexed System Layout
Objective: Make the eight defining elements explicit as a navigable system and prepare transition into system behavior

Layout Logic:
• Entry zone (left static index anchor):
Primary element:

* Headline: A decision system is defined through eight elements

• Processing zone (right segmented indexed field):
Supporting elements:

* 1. Decision — The specific question being resolved, clearly bounded
* 2. Trigger — The condition that initiates the decision
* 3. Inputs — The exact information used for evaluation
* 4. Logic — The rules used to interpret inputs and reach a conclusion
* 5. Ownership — The single role accountable for the decision
* 6. Workflow — The sequence from trigger to execution
* 7. Action — The predefined response following the decision
* 8. Feedback — The mechanism for tracking outcomes and refining the decision

Supporting anchor:

* Each element is distinct. Together, they define how the decision operates.

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to System behavior

Hierarchy:
• Left-side headline remains fixed as the structural anchor
• Eight elements occupy dominant right field in indexed sequence
• Items are segmented into 2 vertical bands of 4, not one long list
• Supporting anchor sits below the indexed field as system summary
• Exit control is isolated at lower-right edge only

Interaction:
• Scroll: free with light snap between indexed bands
• Hover: optional emphasis on single indexed item
• Click: none required; indexed items are not separate destinations
• Content exposure logic: static, all eight elements visible within structured scan field

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on lower-right continuation control
• No lateral branches
• Next destination: System behavior

Transition Role:
• Converts category definition into explicit structural specification
• Pushes from system components into how the structure behaves when aligned or implicit

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT STATIC INDEX ANCHOR ]

Structure

A DECISION SYSTEM IS DEFINED
THROUGH EIGHT ELEMENTS



[ PROCESSING ZONE — RIGHT SEGMENTED INDEXED FIELD ]

┌────────────────────────────────────────────┐    ┌────────────────────────────────────────────┐
│ 1. Decision                                │    │ 5. Ownership                              │
│ The specific question being resolved,      │    │ The single role accountable for the       │
│ clearly bounded                            │    │ decision                                  │
└────────────────────────────────────────────┘    └────────────────────────────────────────────┘

┌────────────────────────────────────────────┐    ┌────────────────────────────────────────────┐
│ 2. Trigger                                 │    │ 6. Workflow                               │
│ The condition that initiates the decision  │    │ The sequence from trigger to execution    │
└────────────────────────────────────────────┘    └────────────────────────────────────────────┘

┌────────────────────────────────────────────┐    ┌────────────────────────────────────────────┐
│ 3. Inputs                                  │    │ 7. Action                                 │
│ The exact information used for evaluation  │    │ The predefined response following the     │
│                                            │    │ decision                                  │
└────────────────────────────────────────────┘    └────────────────────────────────────────────┘

┌────────────────────────────────────────────┐    ┌────────────────────────────────────────────┐
│ 4. Logic                                   │    │ 8. Feedback                               │
│ The rules used to interpret inputs and     │    │ The mechanism for tracking outcomes and   │
│ reach a conclusion                         │    │ refining the decision                     │
└────────────────────────────────────────────┘    └────────────────────────────────────────────┘



[ SUPPORTING ANCHOR ]

Each element is distinct.
Together, they define how the decision operates.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
System behavior
```

 
_______________

SECTION: System behavior
Page: Decision Systems Design
Pattern: Causality → 3-Zone Chain
Objective: Show how aligned structure produces consistent outcomes and how implicit elements introduce variation

Layout Logic:
• Entry zone (left premise field):
Primary element:

* Headline: The structure determines how the decision behaves

• Processing zone (dominant center mechanism field):
Supporting elements:

* When all elements are specified and aligned:
* the same situation leads to the same decision
* the same decision leads to the same action
* outcomes can be explained and improved

• Exit / transition zone (right consequence endpoint):
Control element:

* When any element is implicit, variation enters the system.
* Forward progression to next page: How Decisions Are Structured

Hierarchy:
• Headline anchors the section from the left as the causal premise
• Center zone is dominant and carries the aligned-system logic as sequential mechanism
• Right zone isolates the failure consequence as the endpoint condition
• Forward control is attached only to the right endpoint to preserve one directional path

Interaction:
• Scroll: progressive reveal left → center → right, with end snap on consequence block
• Hover: optional emphasis on each center-line mechanism step
• Click: next-page control clickable only at endpoint
• Content exposure logic:

* first: headline
* second: “When all elements are specified and aligned:”
* third: three mechanism lines
* fourth: consequence block + next-page control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on next-page control
• No optional branches
• Next destination: How Decisions Are Structured

Transition Role:
• Converts structural definition into behavioral consequence
• Pushes from what the system contains to how the system is examined in practice

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT PREMISE ]

System behavior

THE STRUCTURE DETERMINES
HOW THE DECISION BEHAVES



[ PROCESSING ZONE — CENTER DOMINANT MECHANISM ]

When all elements are specified
and aligned:

        the same situation
        leads to the same decision

                    ↓

        the same decision
        leads to the same action

                    ↓

        outcomes can be explained
        and improved



[ EXIT / TRANSITION ZONE — RIGHT ENDPOINT ]

When any element is implicit,
variation enters the system.


                    ┌───────────────────────────────┐
                    │ How Decisions Are Structured →│
                    └───────────────────────────────┘
```

 
____________

SECTION: Hero
Page: How Decisions Are Structured
Pattern: Framing → Bounded Container
Objective: Establish the examination frame for decision structure and shift the user from defined system logic to observed operation in practice

Layout Logic:
• Entry zone (upper-left category anchor):
Primary element:

* Eyebrow: Mechanism
* Headline: Decisions are defined by examining how they actually operate

• Processing zone (bounded central frame):
Supporting elements:

* A decision is not defined by documentation.
* It is defined by how it works in practice.

• Exit / transition zone (lower-right directional release):
Control element:

* Downward progression to Current state

Hierarchy:
• Headline is the dominant anchor and sits at the top edge of the bounded frame
• Eyebrow functions as category cue only
• Two defining statements are contained within a single bounded logic field
• Exit control sits outside the frame at lower-right as the only directional release

Interaction:
• Scroll: free entry, soft snap on bounded frame, release on exit control
• Hover: none
• Click: none inside content; transition control may be clickable
• Content exposure logic: static, with the bounded frame fully visible in one fold

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on transition control
• No optional branches
• Next destination: Current state

Transition Role:
• Repositions the page from abstract structure to practical examination
• Pushes user into the first operational diagnostic step

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Mechanism



[ PROCESSING ZONE — BOUNDED CONTAINER ]

┌──────────────────────────────────────────────────────────────┐
│ DECISIONS ARE DEFINED BY EXAMINING HOW THEY ACTUALLY        │
│ OPERATE                                                      │
│                                                              │
│ A decision is not defined by documentation.                  │
│                                                              │
│ It is defined by how it works in practice.                   │
└──────────────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Current state
```

 
____________

SECTION: Current state
Page: How Decisions Are Structured
Pattern: Situational → Entry Trigger Field
Objective: Define the operational starting point by surfacing the actual decision conditions that must be examined first

Layout Logic:
• Entry zone (left anchor field):
Primary element:

* Headline: Start from actual behavior

• Processing zone (right grouped trigger field):
Supporting elements:

* What triggers the decision
* What inputs are used
* How logic is applied
* Who actually decides
* What action follows
* Whether outcomes are tracked

Supporting anchor:

* This reveals how the decision operates today.

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to Structural clarity

Hierarchy:
• Headline remains isolated on the left as entry anchor
• Six examination points are grouped in a dominant right-side field as uneven inquiry clusters, not a single list wall
• Supporting anchor is detached below the field as the interpretive result
• Transition control stays isolated at lower-right edge as the only next movement

Interaction:
• Scroll: free with soft snap on grouped field
• Hover: optional emphasis on individual inquiry lines
• Click: none on inquiry lines
• Content exposure logic: static, with grouped field fully visible in one section scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Structural clarity

Transition Role:
• Turns the framing premise into an examination field
• Pushes user from practical observation into explicit structural visibility

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT ANCHOR ]

Current state

START FROM
ACTUAL BEHAVIOR



[ PROCESSING ZONE — RIGHT GROUPED TRIGGER FIELD ]

┌──────────────────────────────────┐      ┌───────────────────────────────┐
│ What triggers the decision      │      │ What inputs are used         │
└──────────────────────────────────┘      └───────────────────────────────┘

        ┌──────────────────────────────────┐      ┌───────────────────────────────┐
        │ How logic is applied            │      │ Who actually decides         │
        └──────────────────────────────────┘      └───────────────────────────────┘

┌──────────────────────────────────┐      ┌───────────────────────────────┐
│ What action follows             │      │ Whether outcomes are tracked  │
└──────────────────────────────────┘      └───────────────────────────────┘



[ SUPPORTING ANCHOR ]

This reveals how the decision operates today.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Structural clarity
```

 
________________

SECTION: Structural clarity
Page: How Decisions Are Structured
Pattern: Contrast → Asymmetric Split
Objective: Make the decision structure explicit and separate actual operating elements from adjacent but non-equivalent artifacts

Layout Logic:
• Entry zone (left anchor field):
Primary element:

* Headline: Make the decision structure visible
* Each element of the decision is identified and specified.

• Processing zone (right dominant contrast field):
Supporting elements:

* intended process from actual behavior
* available data from decision inputs
* discussion from decision logic

Framing line:

* This separates:

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to Gap identification

Hierarchy:
• Headline anchors the section on the left with the definitional statement directly beneath it
• “This separates:” introduces the dominant right-side comparison field
• Three contrast pairs are arranged as offset horizontal splits, not stacked bullets
• Transition control sits alone at the lower-right edge to enforce one-direction movement

Interaction:
• Scroll: progressive reveal with soft snap on each contrast row
• Hover: optional emphasis on one contrast row at a time
• Click: none on contrast rows
• Content exposure logic:

* first: headline + defining statement
* second: “This separates:”
* third: first two contrast rows
* fourth: third contrast row + continuation control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Gap identification

Transition Role:
• Converts observation into explicit structural separation
• Pushes user from visibility of structure to traceability of variability

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT ANCHOR ]

Structural clarity

MAKE THE DECISION
STRUCTURE VISIBLE

Each element of the decision
is identified and specified.



[ PROCESSING ZONE — RIGHT DOMINANT CONTRAST FIELD ]

This separates:

┌───────────────────────────────┐      ┌───────────────────────────────┐
│ intended process              │  /   │ actual behavior               │
└───────────────────────────────┘      └───────────────────────────────┘

        ┌───────────────────────────────┐      ┌───────────────────────────────┐
        │ available data                │  /   │ decision inputs               │
        └───────────────────────────────┘      └───────────────────────────────┘

┌───────────────────────────────┐      ┌───────────────────────────────┐
│ discussion                    │  /   │ decision logic                │
└───────────────────────────────┘      └───────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Gap identification
```

 
___________

SECTION: Gap identification
Page: How Decisions Are Structured
Pattern: Breakdown → Fragmented Grid
Objective: Expose where variability appears once the decision structure is visible and prepare movement into diagnostic necessity

Layout Logic:
• Entry zone (upper-left anchor):
Primary element:

* Headline: Variability becomes traceable
* Once the structure is visible, it becomes clear:

• Processing zone (dominant fragmented grid):
Supporting elements:

* where decisions diverge
* where logic changes
* where ownership is unclear
* where outcomes are not connected back to decisions

• Exit / transition zone (lower-right endpoint):
Control element:

* Variability is no longer hidden.
* Forward progression to Diagnostic necessity

Hierarchy:
• Headline anchors the section and carries the interpretive frame
• Lead-in line sits directly below as the grid trigger
• Four variability points are distributed across an uneven 2x2 fragmented field, not a vertical stack
• Closing line is isolated as the endpoint conclusion
• Transition control is attached only to the endpoint block

Interaction:
• Scroll: progressive reveal across the fragmented grid, end snap on conclusion block
• Hover: optional emphasis on one variability block at a time
• Click: none on variability blocks
• Content exposure logic:

* first: headline
* second: lead-in line
* third: four variability blocks
* fourth: conclusion block + continuation control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Diagnostic necessity

Transition Role:
• Converts structural visibility into traceable failure points
• Pushes user from identified gaps into the need for structured diagnostic assessment

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Gap identification

VARIABILITY BECOMES
TRACEABLE

Once the structure is visible,
it becomes clear:



[ PROCESSING ZONE — DOMINANT FRAGMENTED GRID ]

┌──────────────────────────────────┐        ┌──────────────────────────────────┐
│ where decisions diverge          │        │ where logic changes              │
└──────────────────────────────────┘        └──────────────────────────────────┘

           ┌──────────────────────────────────┐        ┌────────────────────────────────────────────┐
           │ where ownership is unclear       │        │ where outcomes are not connected back     │
           │                                  │        │ to decisions                              │
           └──────────────────────────────────┘        └────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

┌──────────────────────────────────┐
│ Variability is no longer hidden. │
└──────────────────────────────────┘

                         ↓
                Diagnostic necessity
```

 
_____________


SECTION: Diagnostic necessity
Page: How Decisions Are Structured
Pattern: Evaluation → Binary Decision Frame
Objective: Establish why diagnostic assessment must precede redesign or implementation and lock the user into a single valid next-step interpretation

Layout Logic:
• Entry zone (left evaluative anchor):
Primary element:

* Headline: The first step is to determine how decisions currently operate
* Before redesign or implementation,
* the organization must understand:

• Processing zone (center requirement field):
Supporting elements:

* which recurring decisions matter
* how they currently function
* where structure is missing

Supporting frame:

* This cannot be derived from reports, process documentation, or internal alignment sessions.
* The decision structure is distributed across teams, systems, and individual judgment,
* and is not visible without a structured assessment.
* Internal teams work within the same implicit structure they are trying to evaluate,
* which makes gaps difficult to isolate objectively.

• Exit / transition zone (right decision endpoint):
Control element:

* Without a diagnostic:
* structural gaps remain hidden
* interventions target symptoms, not causes
* redesign efforts reinforce existing inconsistency
* The diagnostic establishes a clear, externalized view of how decisions actually operate
* and creates the basis required before any change can hold.
* Forward progression to Transition

Hierarchy:
• Headline dominates as the evaluative frame on the left
• Required-understanding items are grouped in the center as the minimum decision basis
• Constraint statements remain compressed beneath the requirement field, not expanded into a full-width block
• “Without a diagnostic:” introduces the right-side endpoint frame
• Final two lines are isolated as the terminal decision outcome and attach directly to the next-section control

Interaction:
• Scroll: progressive reveal left → center → right, with firm snap on endpoint frame
• Hover: optional emphasis on one requirement or consequence line at a time
• Click: none within content; continuation control only at endpoint
• Content exposure logic:

* first: headline + “Before redesign or implementation”
* second: three required-understanding lines
* third: structured-assessment constraint lines
* fourth: “Without a diagnostic” consequences + final basis statement + continuation control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Transition

Transition Role:
• Closes the diagnostic justification with a forced evaluation outcome
• Pushes user from necessity of assessment to the concluding transition into consistent decisions

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT EVALUATIVE ANCHOR ]

Diagnostic necessity

THE FIRST STEP IS TO
DETERMINE HOW DECISIONS
CURRENTLY OPERATE

Before redesign or implementation,

the organization must understand:



[ PROCESSING ZONE — CENTER REQUIREMENT FIELD ]

┌──────────────────────────────────────┐
│ which recurring decisions matter    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ how they currently function         │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ where structure is missing          │
└──────────────────────────────────────┘


This cannot be derived from reports,
process documentation, or internal alignment sessions.

The decision structure is distributed across teams,
systems, and individual judgment,

and is not visible without a structured assessment.

Internal teams work within the same implicit structure
they are trying to evaluate,

which makes gaps difficult to isolate objectively.



[ EXIT / TRANSITION ZONE — RIGHT DECISION ENDPOINT ]

Without a diagnostic:

• structural gaps remain hidden
• interventions target symptoms, not causes
• redesign efforts reinforce existing inconsistency


┌──────────────────────────────────────────────────────┐
│ The diagnostic establishes a clear, externalized    │
│ view of how decisions actually operate              │
│ and creates the basis required before any change    │
│ can hold.                                           │
└──────────────────────────────────────────────────────┘

                              ↓
                           Transition
```

 
______________


SECTION: Transition
Page: How Decisions Are Structured
Pattern: Transition → Forward Push
Objective: Convert diagnostic necessity into applied consistency and direct the user into proof of structure in practice

Layout Logic:
• Entry zone (upper-left transition anchor):
Primary element:

* Headline: From structural visibility to consistent decisions

• Processing zone (center forward movement field):
Supporting elements:

* Once the structure of a decision is visible and defined,
* the same conditions can be evaluated the same way across teams.

• Exit / transition zone (lower-right endpoint):
Control element:

* This is where consistency begins to emerge in practice.
* Forward progression to next page: Decision Systems in Practice

Hierarchy:
• Headline is isolated as the transition anchor
• First supporting line establishes the entry condition in the center field
• Second supporting line carries the forward mechanism and sits further right to create directional pull
• Final line is isolated as the endpoint conclusion
• Next-page control is attached only to the endpoint block

Interaction:
• Scroll: progressive reveal with end snap on endpoint block
• Hover: none
• Click: next-page control clickable only at endpoint
• Content exposure logic:

* first: headline
* second: opening condition
* third: consistency line
* fourth: endpoint statement + next-page control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on next-page control
• No optional paths
• Next destination: Decision Systems in Practice

Transition Role:
• Closes the mechanism page by converting visibility into practical consistency
• Pushes user from diagnosis into proof

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Transition

FROM STRUCTURAL VISIBILITY
TO CONSISTENT DECISIONS



[ PROCESSING ZONE — CENTER FORWARD FIELD ]

Once the structure of a decision
is visible and defined,

                    the same conditions can be evaluated
                    the same way across teams.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ENDPOINT ]

This is where consistency
begins to emerge in practice.


                    ┌────────────────────────────────────┐
                    │ Decision Systems in Practice    →  │
                    └────────────────────────────────────┘
```

 
_____________


SECTION: Hero
Page: Decision Systems in Practice
Pattern: Framing → Bounded Container
Objective: Anchor the proof page around one recurring decision and frame structure as the source of the difference

Layout Logic:
• Entry zone (upper-left category anchor):
Primary element:

* Eyebrow: Proof
* Headline: Customer retention intervention as a defined decision

• Processing zone (bounded central proof frame):
Supporting elements:

* A recurring decision: when to intervene with an at-risk customer.
* The difference is not data.
* It is how the decision is structured.

• Exit / transition zone (lower-right directional release):
Control element:

* Downward progression to Before

Hierarchy:
• Headline is the dominant anchor and defines the proof case
• Eyebrow functions as category cue only
• The recurring decision line sits first inside the bounded frame as case definition
• The final two lines are separated within the frame as the proof distinction
• Exit control remains outside the frame at lower-right as the only forward action

Interaction:
• Scroll: free entry, soft snap on bounded frame, release on exit
• Hover: none
• Click: none inside frame; transition control may be clickable
• Content exposure logic: static, full proof frame visible in one scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on transition control
• No optional branches
• Next destination: Before

Transition Role:
• Converts abstract structural logic into a concrete applied case
• Pushes user from framed proof into unstructured baseline state

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Proof



[ PROCESSING ZONE — BOUNDED CONTAINER ]

┌──────────────────────────────────────────────────────────────┐
│ CUSTOMER RETENTION INTERVENTION AS A DEFINED DECISION       │
│                                                              │
│ A recurring decision: when to intervene with an at-risk     │
│ customer.                                                   │
│                                                              │
│ The difference is not data.                                 │
│                                                              │
│ It is how the decision is structured.                       │
└──────────────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Before
```

 
____________


SECTION: Before
Page: Decision Systems in Practice
Pattern: Breakdown → Fragmented Grid
Objective: Show how the recurring decision operates without structure by distributing instability across the decision field

Layout Logic:
• Entry zone (upper-left anchor):
Primary element:

* Headline: How the decision operates without structure

• Processing zone (dominant fragmented grid):
Supporting elements:

* No explicit definition of “at-risk customer” (usage decline vs complaints vs inactivity)
* Intervention triggered inconsistently — some accounts reviewed weekly, others only after escalation
* Different teams use different signals (usage metrics, support tickets, account manager judgment)
* No consistent thresholds for when intervention is required
* Multiple stakeholders influence the decision without clear accountability
* Actions vary — some customers receive proactive outreach, others receive no intervention
* Outcomes are not tracked against the decision

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to After

Hierarchy:
• Headline anchors the section and defines the state condition
• Instability points are distributed across an uneven multi-block field, not shown as a single list
• Longest statements occupy wider blocks; shorter failures occupy tighter blocks
• Transition control is isolated at lower-right edge as the only next movement

Interaction:
• Scroll: progressive reveal across grid with soft snap on final row
• Hover: optional emphasis on individual failure blocks
• Click: none on blocks
• Content exposure logic:

* first: headline
* second: first row of instability blocks
* third: middle row
* fourth: final row + continuation control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: After

Transition Role:
• Establishes the unstructured baseline state
• Pushes user from fragmented practice into defined system reconstruction

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Before

HOW THE DECISION OPERATES
WITHOUT STRUCTURE



[ PROCESSING ZONE — DOMINANT FRAGMENTED GRID ]

┌──────────────────────────────────────────────────────┐    ┌──────────────────────────────────────────────┐
│ No explicit definition of “at-risk customer”        │    │ No consistent thresholds for when           │
│ (usage decline vs complaints vs inactivity)          │    │ intervention is required                    │
└──────────────────────────────────────────────────────┘    └──────────────────────────────────────────────┘

        ┌────────────────────────────────────────────────────────────┐    ┌──────────────────────────────────────────────┐
        │ Intervention triggered inconsistently — some accounts      │    │ Multiple stakeholders influence the         │
        │ reviewed weekly, others only after escalation              │    │ decision without clear accountability       │
        └────────────────────────────────────────────────────────────┘    └──────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐    ┌──────────────────────────────────────────────────────┐
│ Different teams use different signals (usage metrics,     │    │ Actions vary — some customers receive proactive     │
│ support tickets, account manager judgment)                │    │ outreach, others receive no intervention            │
└────────────────────────────────────────────────────────────┘    └──────────────────────────────────────────────────────┘

                           ┌──────────────────────────────────────────────┐
                           │ Outcomes are not tracked against            │
                           │ the decision                                │
                           └──────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
After
```

 
_______________

SECTION: After
Page: Decision Systems in Practice
Pattern: Alignment → Vertical Consistency Stack
Objective: Show the same decision as an explicitly defined system and prepare movement into measurable result

Layout Logic:
• Entry zone (upper-left anchor):
Primary element:

* Headline: The same decision defined as a system

• Processing zone (dominant vertical consistency stack):
Supporting elements:

* Decision defined: which customers require intervention in the current weekly cycle
* Trigger activated when usage drops below defined thresholds or risk score exceeds a set level
* Standardized input set combining product usage, support activity, and account signals
* Explicit logic defines intervention type based on severity and customer segment
* Single ownership assigned to a defined role responsible for the decision
* Consistent workflow from trigger → evaluation → action
* Predefined intervention actions (outreach, escalation, retention offer)
* Outcomes tracked against intervention type and timing

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to Result

Hierarchy:
• Headline anchors the section as the defined-state condition
• Eight structural lines are arranged as a descending consistency stack, each line isolated as one system layer
• Earlier layers carry definition and trigger; middle layers carry logic and ownership; lower layers carry action and feedback
• Transition control is isolated below the final layer as the only forward action

Interaction:
• Scroll: progressive reveal top → bottom with light snap between stack layers
• Hover: optional emphasis on individual stack layer
• Click: none on stack layers
• Content exposure logic:

* first: headline + first three layers
* second: middle three layers
* third: final two layers + continuation control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Result

Transition Role:
• Reconstructs the fragmented baseline into an aligned operating system
• Pushes user from defined structure into outcome change

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

After

THE SAME DECISION
DEFINED AS A SYSTEM



[ PROCESSING ZONE — DOMINANT VERTICAL CONSISTENCY STACK ]

┌──────────────────────────────────────────────────────────────────────┐
│ Decision defined: which customers require intervention in the       │
│ current weekly cycle                                                │
└──────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────────────────────────────┐
        │ Trigger activated when usage drops below defined thresholds │
        │ or risk score exceeds a set level                           │
        └──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│ Standardized input set combining product usage, support activity,   │
│ and account signals                                                 │
└──────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────────────────────────────┐
        │ Explicit logic defines intervention type based on severity  │
        │ and customer segment                                        │
        └──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│ Single ownership assigned to a defined role responsible for the     │
│ decision                                                            │
└──────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────────────────────────────┐
        │ Consistent workflow from trigger → evaluation → action      │
        └──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│ Predefined intervention actions (outreach, escalation, retention    │
│ offer)                                                              │
└──────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────────────────────────────┐
        │ Outcomes tracked against intervention type and timing       │
        └──────────────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Result
```

 
_____________

SECTION: Result
Page: Decision Systems in Practice
Pattern: Evidence → Compressed Density Block
Objective: Compress the outcome shift into a high-clarity proof block and direct the user into commitment

Layout Logic:
• Entry zone (upper-left anchor):
Primary element:

* Headline: What changes when the decision is structured

• Processing zone (compressed central evidence block):
Supporting elements:

* The same conditions trigger the same decision
* Teams act consistently across accounts
* Interventions occur earlier and under defined conditions
* Outcomes are measurable and comparable across teams

• Exit / transition zone (lower-right endpoint):
Control element:

* Forward progression to Where does your decision structure break
* CTA path: Request a Decision Diagnostic

Hierarchy:
• Headline anchors the section as the proof summary
• Four outcome lines are compressed into a narrow central density block for fast scan and high retention
• No internal subhierarchy inside the evidence block; each line carries equal proof weight
• Transition and CTA are isolated below and to the right to separate evidence from action

Interaction:
• Scroll: free with soft snap on evidence block and firmer snap on CTA endpoint
• Hover: optional emphasis on one outcome line at a time
• Click: CTA control clickable
• Content exposure logic: static evidence block first, CTA endpoint enters on continued scroll

Navigation Behavior:
• Primary path is linear forward progression
• Transition triggered by scroll into CTA endpoint or direct click on CTA
• Supporting path available only through CTA action
• Next destination: Decision Diagnostic

Transition Role:
• Converts proof into decision pressure
• Pushes user from validated outcome change into commitment action

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Result

WHAT CHANGES WHEN
THE DECISION IS STRUCTURED



[ PROCESSING ZONE — COMPRESSED CENTRAL EVIDENCE BLOCK ]

┌────────────────────────────────────────────────────┐
│ The same conditions trigger the same decision      │
│                                                    │
│ Teams act consistently across accounts             │
│                                                    │
│ Interventions occur earlier and under defined      │
│ conditions                                         │
│                                                    │
│ Outcomes are measurable and comparable across      │
│ teams                                              │
└────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

Where does your decision
structure break

┌────────────────────────────────────┐
│ Request a Decision Diagnostic   →  │
└────────────────────────────────────┘
```

 
_______________


SECTION: Hero
Page: Decision Diagnostic
Pattern: Commitment → Single-Path Lock
Objective: Convert validated inconsistency into one concrete next step and lock the page around diagnostic commitment

Layout Logic:
• Entry zone (left commitment anchor):
Primary element:

* Eyebrow: Decision Diagnostic
* Headline: Determine how your decisions actually operate

• Processing zone (right commitment field):
Supporting elements:

* If decisions produce inconsistent outcomes,
* the structure is not fully defined.
* The diagnostic identifies how your most important recurring decisions function today
* and where structural gaps exist.

• Exit / transition zone (lower-right locked action):
Control element:

* Downward progression to Scope
* Primary CTA path remains locked to Request a Decision Diagnostic at page end

Hierarchy:
• Headline dominates as the commitment anchor
• Eyebrow functions as category signal only
• Supporting lines are grouped into a right-side commitment field, with the first two lines establishing necessity and the last two lines defining what the diagnostic does
• Exit control is isolated at lower-right and preserves one directional path into the rest of the page

Interaction:
• Scroll: free entry, soft snap on headline, firmer snap on exit control
• Hover: none
• Click: none inside content; transition control may be clickable
• Content exposure logic: static, full commitment field visible in one scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on transition control
• No optional branches
• Next destination: Scope
• Global action path remains single and consistent: Request a Decision Diagnostic

Transition Role:
• Converts proof into explicit diagnostic commitment
• Pushes user from why structure matters to what the diagnostic examines

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT COMMITMENT ANCHOR ]

Decision Diagnostic

DETERMINE HOW YOUR
DECISIONS ACTUALLY
OPERATE



[ PROCESSING ZONE — RIGHT COMMITMENT FIELD ]

If decisions produce
inconsistent outcomes,

the structure is not
fully defined.


The diagnostic identifies how your most
important recurring decisions function today

and where structural gaps exist.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Scope
```

 
___________

SECTION: Scope
Page: Decision Diagnostic
Pattern: Structure → Indexed System Layout
Objective: Define exactly what the diagnostic examines and keep the page in controlled forward progression toward why this step matters

Layout Logic:
• Entry zone (left static anchor):
Primary element:

* Headline: What is examined

• Processing zone (right indexed field):
Supporting elements:

* decision definition
* triggers
* inputs
* logic
* ownership
* workflow
* action
* feedback
* Across selected high-impact decisions.

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to Why this step

Hierarchy:
• Headline remains isolated on the left as the structural anchor
• Eight examined elements occupy the dominant right field as an indexed scan system
• “Across selected high-impact decisions.” is detached below the indexed field as the scope qualifier
• Transition control is isolated at the lower-right edge as the only next movement

Interaction:
• Scroll: free with light snap between indexed rows
• Hover: optional emphasis on one indexed item at a time
• Click: none on indexed items
• Content exposure logic: static, with full indexed field visible as one structured scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Why this step

Transition Role:
• Converts diagnostic commitment into explicit examination scope
• Pushes user from what is examined to why this step must come first

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT STATIC ANCHOR ]

Scope

WHAT IS EXAMINED



[ PROCESSING ZONE — RIGHT INDEXED FIELD ]

┌──────────────────────────────┐      ┌──────────────────────────────┐
│ 1. decision definition       │      │ 5. ownership                │
└──────────────────────────────┘      └──────────────────────────────┘

┌──────────────────────────────┐      ┌──────────────────────────────┐
│ 2. triggers                  │      │ 6. workflow                 │
└──────────────────────────────┘      └──────────────────────────────┘

┌──────────────────────────────┐      ┌──────────────────────────────┐
│ 3. inputs                    │      │ 7. action                   │
└──────────────────────────────┘      └──────────────────────────────┘

┌──────────────────────────────┐      ┌──────────────────────────────┐
│ 4. logic                     │      │ 8. feedback                 │
└──────────────────────────────┘      └──────────────────────────────┘



[ SCOPE QUALIFIER ]

Across selected high-impact decisions.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Why this step
```

 
____________


SECTION: Why this step
Page: Decision Diagnostic
Pattern: Evaluation → Binary Decision Frame
Objective: Establish the diagnostic as the necessary baseline before any redesign or implementation and direct the user into defined output

Layout Logic:
• Entry zone (left evaluative anchor):
Primary element:

* Headline: This precedes any redesign or implementation
* Without a clear view of how decisions currently operate,

• Processing zone (center consequence field):
Supporting elements:

* priorities are unclear
* gaps are misidentified
* changes do not hold

• Exit / transition zone (right decision endpoint):
Control element:

* The diagnostic establishes the baseline.
* Forward progression to Output

Hierarchy:
• Headline anchors the section as the evaluation frame
• Lead-in condition sits directly beneath and introduces the center consequence field
• Three consequence lines are isolated in the center as equal-weight failure outcomes
• Baseline statement is separated in the right endpoint block as the decision conclusion
• Transition control is attached only to the endpoint block

Interaction:
• Scroll: progressive reveal left → center → right, with end snap on baseline block
• Hover: optional emphasis on one consequence line at a time
• Click: none within consequence field; continuation control only at endpoint
• Content exposure logic:

* first: headline
* second: lead-in condition
* third: three consequence lines
* fourth: baseline statement + continuation control

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll completion or click on continuation control
• No optional paths
• Next destination: Output

Transition Role:
• Closes the justification for diagnostic sequencing
• Pushes user from necessity into expected deliverables

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT EVALUATIVE ANCHOR ]

Why this step

THIS PRECEDES ANY
REDESIGN OR
IMPLEMENTATION

Without a clear view of how
decisions currently operate,



[ PROCESSING ZONE — CENTER CONSEQUENCE FIELD ]

┌──────────────────────────────┐
│ priorities are unclear       │
└──────────────────────────────┘

┌──────────────────────────────┐
│ gaps are misidentified       │
└──────────────────────────────┘

┌──────────────────────────────┐
│ changes do not hold          │
└──────────────────────────────┘



[ EXIT / TRANSITION ZONE — RIGHT DECISION ENDPOINT ]

┌──────────────────────────────────────┐
│ The diagnostic establishes           │
│ the baseline.                        │
└──────────────────────────────────────┘

                      ↓
                    Output
```

 
_____________


SECTION: Output
Page: Decision Diagnostic
Pattern: Evidence → Compressed Density Block
Objective: Compress the diagnostic deliverables into a precise output field and move directly into final commitment

Layout Logic:
• Entry zone (upper-left anchor):
Primary element:

* Headline: What you receive

• Processing zone (compressed central evidence block):
Supporting elements:

* mapped decision structures for selected high-impact decisions
* explicit identification of variability points across teams and workflows
* breakdown of structural gaps across decision, trigger, inputs, logic, ownership, workflow, action, and feedback
* prioritized set of decisions requiring definition
* documented decision boundaries and ownership assignments
* defined starting point for decision system design

• Exit / transition zone (lower-right commitment endpoint):
Control element:

* Request a Decision Diagnostic

Hierarchy:
• Headline anchors the section as the output definition
• Deliverables are compressed into a narrow central density block for fast scan and retention
• Longer deliverables occupy full-width lines inside the block; shorter deliverables remain isolated without sub-grouping
• CTA is separated below and to the right as the only allowed next action

Interaction:
• Scroll: free with soft snap on evidence block and firmer snap on CTA endpoint
• Hover: optional emphasis on one deliverable line at a time
• Click: CTA clickable
• Content exposure logic: static evidence block first, commitment endpoint enters on continued scroll

Navigation Behavior:
• Linear progression with single commitment path
• Transition triggered by scroll into CTA endpoint or direct click on CTA
• No optional branches
• Next destination: Request a Decision Diagnostic

Transition Role:
• Converts defined output into action readiness
• Ends the page in a single-path commitment state

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Output

WHAT YOU RECEIVE



[ PROCESSING ZONE — COMPRESSED CENTRAL EVIDENCE BLOCK ]

┌──────────────────────────────────────────────────────────────┐
│ mapped decision structures for selected high-impact         │
│ decisions                                                    │
│                                                              │
│ explicit identification of variability points across teams  │
│ and workflows                                               │
│                                                              │
│ breakdown of structural gaps across decision, trigger,      │
│ inputs, logic, ownership, workflow, action, and feedback    │
│                                                              │
│ prioritized set of decisions requiring definition           │
│                                                              │
│ documented decision boundaries and ownership assignments    │
│                                                              │
│ defined starting point for decision system design           │
└──────────────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

┌────────────────────────────────────┐
│ Request a Decision Diagnostic   →  │
└────────────────────────────────────┘
```

 
_____________


SECTION: Hero
Page: Insights
Pattern: Framing → Bounded Container
Objective: Establish the page as a reinforcement layer for recurring decision failure patterns and direct the user into structural topics

Layout Logic:
• Entry zone (upper-left category anchor):
Primary element:

* Eyebrow: Insights
* Headline: Decision systems thinking

• Processing zone (bounded central frame):
Supporting elements:

* Recurring decisions fail in consistent ways.
* These patterns reveal how decisions actually operate.

• Exit / transition zone (lower-right directional release):
Control element:

* Downward progression to Key topics

Hierarchy:
• Headline is the dominant anchor and defines the page frame
• Eyebrow functions as category cue only
• Two supporting lines are contained within a single bounded logic field
• Exit control remains outside the frame at lower-right as the only forward action

Interaction:
• Scroll: free entry, soft snap on bounded frame, release on exit
• Hover: none
• Click: none inside frame; transition control may be clickable
• Content exposure logic: static, full frame visible in one scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on transition control
• No optional branches
• Next destination: Key topics

Transition Role:
• Frames Insights as a structural reinforcement page
• Pushes user from page definition into topic-level pattern field

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Insights



[ PROCESSING ZONE — BOUNDED CONTAINER ]

┌──────────────────────────────────────────────────────────────┐
│ DECISION SYSTEMS THINKING                                   │
│                                                              │
│ Recurring decisions fail in consistent ways.                │
│                                                              │
│ These patterns reveal how decisions actually operate.       │
└──────────────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Key topics
```

 
______________


SECTION: Key topics
Page: Insights
Pattern: Structure → Indexed System Layout
Objective: Present the recurring structural patterns as a navigable topic system and direct the user into the unifying question

Layout Logic:
• Entry zone (left static anchor):
Primary element:

* Headline: Structural patterns across decisions

• Processing zone (right indexed topic field):
Supporting elements:

* 1. Decision inconsistency across teams
* 2. Data does not align decisions
* 3. Ownership determines outcomes
* 4. Logic varies under the same conditions
* 5. Reporting does not translate into action

• Exit / transition zone (lower-right continuation control):
Control element:

* Forward progression to Direction

Hierarchy:
• Headline remains isolated on the left as the structural anchor
• Five topics occupy the dominant right field as indexed scan units
• Topics are arranged in uneven two-column distribution to avoid a single list wall
• Transition control is isolated at the lower-right edge as the only next movement

Interaction:
• Scroll: free with light snap between topic rows
• Hover: emphasis on individual topic unit
• Click: topic units clickable as optional supporting paths
• Content exposure logic: static, all five topics visible in one structured scan

Navigation Behavior:
• Primary navigation is linear through scroll to Direction
• Optional supporting paths available through click on each topic unit
• Transition triggered by scroll completion or click on continuation control
• Next destination: Direction

Transition Role:
• Converts page framing into a structured field of recurring patterns
• Pushes user from indexed topics to the single diagnostic question beneath them

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT STATIC ANCHOR ]

Key topics

STRUCTURAL PATTERNS
ACROSS DECISIONS



[ PROCESSING ZONE — RIGHT INDEXED TOPIC FIELD ]

┌────────────────────────────────────┐      ┌────────────────────────────────────┐
│ 1. Decision inconsistency          │      │ 3. Ownership determines           │
│    across teams                    │      │    outcomes                       │
└────────────────────────────────────┘      └────────────────────────────────────┘

┌────────────────────────────────────┐      ┌────────────────────────────────────┐
│ 2. Data does not                   │      │ 4. Logic varies under             │
│    align decisions                 │      │    the same conditions            │
└────────────────────────────────────┘      └────────────────────────────────────┘

                     ┌──────────────────────────────────────────────┐
                     │ 5. Reporting does not translate into action │
                     └──────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Direction
```

 
____________


SECTION: Direction
Page: Insights
Pattern: Definition → Anchored Block
Objective: Collapse all topic variation into one governing diagnostic question and end the page in a single interpretive frame

Layout Logic:
• Entry zone (dominant anchored core):
Primary element:

* Headline: Each pattern points to one question

• Processing zone (offset lower-right core definition):
Supporting elements:

* How is the decision structured?
* Understanding this determines whether outcomes can be made consistent.

• Exit / transition zone (lower-right endpoint):
Control element:

* No lateral topic branching at this level
* Return path remains via page-level navigation only

Hierarchy:
• Headline is the dominant anchor and occupies primary visual weight
• “How is the decision structured?” is isolated as the core diagnostic line
• “Understanding this determines whether outcomes can be made consistent.” sits beneath as the consequence frame
• Exit state is non-promotional and closes the page without introducing a new CTA

Interaction:
• Scroll: free entry, soft snap on anchored core, release at section end
• Hover: none
• Click: none inside section
• Content exposure logic: static, full definition visible in one scan

Navigation Behavior:
• Linear closure of the page narrative
• No in-section optional paths
• Transition triggered by end-of-page scroll completion
• Further movement occurs through global/page-level navigation only

Transition Role:
• Resolves the page into one governing question
• Closes reinforcement without dispersing attention

ASCII WIREFRAME:

```text
[ ENTRY ZONE — ANCHORED CORE ]

Direction

EACH PATTERN POINTS
TO ONE QUESTION



[ PROCESSING ZONE — OFFSET CORE DEFINITION ]

                         How is the decision
                         structured?

                         Understanding this determines
                         whether outcomes can be
                         made consistent.



[ EXIT / TRANSITION ZONE — END STATE ]

                         End of page
```

 
_______________


SECTION: Hero
Page: About
Pattern: Definition → Anchored Block
Objective: Establish the scope of work as singular and bounded, then move into the positioning layer

Layout Logic:
• Entry zone (dominant anchored core on left):
Primary element:

* Eyebrow: About
* Headline: Focused on decision systems

• Processing zone (offset right support field):
Supporting elements:

* The work is limited to one problem:
* defining how recurring decisions operate as structured systems.

• Exit / transition zone (lower-right directional release):
Control element:

* Downward progression to Position

Hierarchy:
• Headline is the primary anchor and carries the section weight
• Eyebrow acts as category cue only
• Scope statement is split into two short support lines in the offset field
• Exit control is isolated at lower-right as the only forward path

Interaction:
• Scroll: free entry, soft snap on anchor, release on exit
• Hover: none
• Click: none inside content; transition control may be clickable
• Content exposure logic: static, full section visible in one scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on transition control
• No optional branches
• Next destination: Position

Transition Role:
• Defines the boundary of the work immediately
• Pushes user from scope statement into structural positioning

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT DOMINANT ]

About

FOCUSED ON
DECISION SYSTEMS



[ PROCESSING ZONE — RIGHT OFFSET ]

The work is limited
to one problem:

defining how recurring decisions
operate as structured systems.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Position
```

 
_______________


SECTION: Position
Page: About
Pattern: Contrast → Asymmetric Split
Objective: Position decision structure as a distinct layer between investment areas already in place and move into background authority

Layout Logic:
• Entry zone (left lighter premise field):
Primary element:

* Headline: Decision structure as a distinct layer
* Organizations invest in data, reporting, and execution.

• Processing zone (right dominant contrast field):
Supporting elements:

* The decision layer between them is rarely defined.
* This work focuses on that layer.

• Exit / transition zone (lower-right continuation control):
Control element:

* Downward progression to Background

Hierarchy:
• Headline anchors the section and defines the positioning frame
• Left-side premise line provides context only, not equal weight
• Right-side two-line field carries the true positioning claim and dominates attention
• Exit control is isolated at lower-right as the only forward movement

Interaction:
• Scroll: progressive reveal with soft snap at the dominant right field
• Hover: none
• Click: none inside content; transition control may be clickable
• Content exposure logic: static, with all content visible in one scan

Navigation Behavior:
• Linear progression only
• Transition triggered by scroll or click on continuation control
• No optional paths
• Next destination: Background

Transition Role:
• Establishes the distinct positioning of the work
• Pushes user from category position into experience-based authority

ASCII WIREFRAME:

```text
[ ENTRY ZONE — LEFT / LIGHTER ]

Position

DECISION STRUCTURE
AS A DISTINCT LAYER

Organizations invest in data,
reporting, and execution.



[ PROCESSING ZONE — RIGHT / DOMINANT ]

The decision layer between them
is rarely defined.

This work focuses
on that layer.



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

↓
Background
```

 
________________


SECTION: Background
Page: About
Pattern: Evidence → Compressed Density Block
Objective: Establish authority through direct operational experience and end the page in commitment support

Layout Logic:
• Entry zone (upper-left anchor):
Primary element:

* Headline: Experience in decision, data, and operational structures

• Processing zone (compressed central evidence block):
Supporting elements:

* Experience includes direct responsibility for enterprise decision environments
* where data, analytics, and operational workflows intersect,
* including ownership of how decisions are defined, governed, and executed
* across multiple functions and teams.

• Exit / transition zone (lower-right commitment endpoint):
Control element:

* Continue to diagnostic
* Request a Decision Diagnostic

Hierarchy:
• Headline anchors the section as the authority definition
• Four lines are compressed into a narrow evidence block with controlled line length
• The middle lines carry operational context; final line closes scope of authority
• CTA endpoint is separated below and to the right as the only next action

Interaction:
• Scroll: free with soft snap on evidence block and firmer snap on CTA endpoint
• Hover: none
• Click: CTA clickable
• Content exposure logic: static evidence first, commitment endpoint enters on continued scroll

Navigation Behavior:
• Linear progression with single commitment path
• Transition triggered by scroll into CTA endpoint or direct click on CTA
• No optional branches
• Next destination: Request a Decision Diagnostic

Transition Role:
• Converts authority support into action readiness
• Ends the About page in commitment support state

ASCII WIREFRAME:

```text
[ ENTRY ZONE — UPPER LEFT ]

Background

EXPERIENCE IN DECISION,
DATA, AND OPERATIONAL
STRUCTURES



[ PROCESSING ZONE — COMPRESSED CENTRAL EVIDENCE BLOCK ]

┌────────────────────────────────────────────────────┐
│ Experience includes direct responsibility for      │
│ enterprise decision environments                   │
│                                                    │
│ where data, analytics, and operational workflows   │
│ intersect,                                         │
│                                                    │
│ including ownership of how decisions are defined,  │
│ governed, and executed                             │
│                                                    │
│ across multiple functions and teams.               │
└────────────────────────────────────────────────────┘



[ EXIT / TRANSITION ZONE — LOWER RIGHT ]

Continue to diagnostic

┌────────────────────────────────────┐
│ Request a Decision Diagnostic   →  │
└────────────────────────────────────┘
```

 
_________

SECTION: Hero — Entry to structured action
Pattern: Definition → Anchored Block
Objective: Establish entry into a controlled diagnostic process
Layout Logic: Dominant centered anchor with peripheral qualifiers (no symmetry balance)

* Primary element (center anchor):
  Start Decision Diagnostic

* Supporting elements (peripheral left/right):
  You are entering a structured assessment of how your recurring decisions operate today.

* Control element (exit direction):
  ↓ Continue to required input definition

Hierarchy:
Primary → Supporting → Direction

Interaction:
Scroll-triggered continuation only

Transition Role:
Moves from entry clarity → requirement framing

ASCII WIREFRAME:

```
[Entry Zone]

            Start Decision Diagnostic

    You are entering a structured assessment of how your recurring decisions operate today.


[Exit →]
↓ Continue to required input definition
```

---

SECTION: Requirement Framing
Pattern: Constraint → Bounded Container
Objective: Define strict input boundaries and eliminate ambiguity
Layout Logic: Framed block with internal asymmetry (rules vs exclusion)

* Primary element (frame center):
  Identify one recurring decision.

* Supporting elements (left stack):
  Describe where inconsistency appears.
  Provide context for evaluation.

* Supporting elements (right isolated constraint):
  This is not a general inquiry.

* Control element:
  ↓ Proceed to structured input

Hierarchy:
Primary (center) → Supporting (left) → Constraint (right)

Interaction:
None (read-only constraint framing)

Transition Role:
Forces user into correct mental model before input

ASCII WIREFRAME:

```
[Entry Zone]

┌──────────────────────────────────────────────┐
│ Identify one recurring decision.             │
│                                              │
│  Describe where inconsistency appears.       │
│  Provide context for evaluation.             │
│                                              │
│                         This is not a        │
│                         general inquiry.     │
└──────────────────────────────────────────────┘

[Exit →]
↓ Proceed to structured input
```

---

SECTION: Diagnostic Input Form
Pattern: Commitment → Single-Path Lock
Objective: Capture structured diagnostic inputs with enforced discipline
Layout Logic: Asymmetric split (form dominance + guidance rail)

* Primary element (right dominant column):
  FORM

Name
Company
Your role

Which recurring decision produces inconsistent outcomes?
(e.g. pricing adjustments, customer retention intervention, planning allocation)
Focus on one decision.

Where does inconsistency appear?

[ ] across teams
[ ] across regions
[ ] across time
[ ] depends on individuals
[ ] other

How does this decision currently happen?
(What triggers it, who decides, what inputs are used, what action follows)

Email

[ Request Decision Diagnostic ]

* Supporting elements (left narrow rail):
  Focus on one decision.
  No “message” field → forces structure
  No “how can we help?” → breaks positioning
  No secondary CTA → no “contact us”
  No phone field → reduces noise
  No long form → keeps friction controlled

* Control element:
  [ Request Decision Diagnostic ]

Hierarchy:
Form (dominant) → Guidance (secondary) → CTA (terminal lock)

Interaction:
Single-path submission only

Transition Role:
Commits user into system-defined input

ASCII WIREFRAME:

```
[Entry Zone]

| Guidance Rail (30%)           | Form (70%)                          |
|------------------------------|------------------------------------|
| Focus on one decision        | Name                               |
| No “message” field           | Company                            |
| No “how can we help?”        | Your role                          |
| No secondary CTA             |                                    |
| No phone field               | Which recurring decision produces  |
| No long form                 | inconsistent outcomes?             |
|                              | (e.g. pricing adjustments...)      |
|                              | Focus on one decision              |
|                              |                                    |
|                              | Where does inconsistency appear?   |
|                              | [ ] across teams                   |
|                              | [ ] across regions                 |
|                              | [ ] across time                    |
|                              | [ ] depends on individuals         |
|                              | [ ] other                          |
|                              |                                    |
|                              | How does this decision currently   |
|                              | happen?                            |
|                              | (What triggers it...)              |
|                              |                                    |
|                              | Email                              |
|                              |                                    |
|                              | [ Request Decision Diagnostic ]    |

[Exit → Locked action]
```

---

SECTION: Post-Submission Expectation
Pattern: Sequence → Linear Progression
Objective: Clarify what happens next in controlled steps
Layout Logic: Horizontal progression (3-step flow)

* Primary element:
  We review your decision context.

* Supporting elements:
  We confirm scope and fit.
  We initiate a focused diagnostic discussion.

* Control element:
  → Continue to boundary clarification

Hierarchy:
Step 1 → Step 2 → Step 3

Interaction:
Passive scan

Transition Role:
Reduces uncertainty after action

ASCII WIREFRAME:

```
[Entry Zone]

We review your decision context  →  We confirm scope and fit  →  We initiate a focused diagnostic discussion

[Exit →]
→ Continue to boundary clarification
```

---

SECTION: Boundary Enforcement
Pattern: Principle → Vertical Consistency Stack
Objective: Lock positioning and prevent misinterpretation
Layout Logic: Isolated vertical statements (no grouping)

* Primary element:
  This is not a generic consultation.

* Supporting elements:
  The focus is decision structure.
  The output is a defined baseline.
  The goal is clarity before any redesign or implementation.

* Control element:
  (Implicit closure — no alternative path)

Hierarchy:
Primary → Supporting stack

Interaction:
None

Transition Role:
Final framing → reinforces commitment integrity

ASCII WIREFRAME:

```
[Entry Zone]

This is not a generic consultation.

The focus is decision structure.

The output is a defined baseline.

The goal is clarity before any redesign or implementation.


[Exit → End of flow]
```
